apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ip_location.fullname" . }}-test-connection"
  labels:
    {{- include "ip_location.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ip_location.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
